## Summary

## Changes

## Test
- [ ] `pre-commit run --all-files` passes
- [ ] `pre-commit run --hook-stage pre-push --all-files` passes (pytest + mypy)
- [ ] `python main.py` launches and the changed flow works
